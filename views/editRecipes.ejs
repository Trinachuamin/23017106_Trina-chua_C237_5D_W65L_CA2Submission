<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>Simple Recipe Book</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>

<body background="https://i.pinimg.com/564x/7a/d6/f9/7ad6f92eb2af83036d0e013dde7f54ea.jpg">
    <%- include('partials/header') %>

        <div class="container my-5">
            <div class="container-one p-5">
                <div class="text-center">
                    <h2>Update Recipe</h2>
                </div>
                <br>
                <% if (recipe) { %>
                    <form action="/recipes/edit/<%= recipe.recipeid %>" method="POST" enctype="multipart/form-data">
                        <div class="mb-3">
                            <label for="titles" class="form-label">Recipe Title:</label>
                            <input type="text" id="titles" name="titles" class="form-control"
                                value="<%= recipe.titles %>" required>
                        </div>
                        <div class="mb-3">
                            <label for="ingredients" class="form-label">Ingredients:</label>
                            <textarea id="ingredients" name="ingredients" class="form-control" rows="4"
                                required><%= recipe.ingredients %></textarea>
                        </div>
                        <div class="mb-3">
                            <label for="instructions" class="form-label">Instructions:</label>
                            <textarea id="instructions" name="instructions" class="form-control" rows="4"
                                required><%= recipe.instructions %></textarea>
                        </div>
                        <div class="mb-3">
                            <label for="categories" class="form-label">Categories:</label>
                            <input type="text" id="categories" name="categories" class="form-control"
                                value="<%= recipe.categories %>" required>
                        </div>
                        <div class="mb-3">
                            <label for="image" class="form-label">Current Image:</label>
                            <input type="hidden" name="currentImage" value="<%= recipe.images %>">
                            <input type="text" name="currentImage" class="form-control" value="<%= recipe.images %>"
                                disabled>
                            <img src="/img/<%= recipe.images %>" class="img-fluid" style="max-width: 20%;">
                        </div>
                        <div class="mb-3">
                            <label for="image" class="form-label">New Image:</label>
                            <input type="file" id="image" name="image" class="form-control" accept="image/*">
                        </div>
                        <button type="submit" class="btn btn-primary btn-sm custom-btn">Update Recipe</button>
                    </form>
                    <% } else { %>
                        <p>No recipe found.</p>
                        <% } %>
                            <br>
                            <a href="/" class="btn btn-secondary btn-sm custom-btn">Back</a>
            </div>
</body>

</html>